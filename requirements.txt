# Core ML/CV libraries
ultralytics==8.2.0
opencv-python==4.9.0.80

# Streamlit web app
streamlit==1.31.0

# Keep Torch below 2.6 to avoid checkpoint-loading incompatibilities
# with older Ultralytics releases that rely on full-object deserialization.
torch<2.6
torchvision<0.21

# Optional: For advanced features
# tensorrt  # Uncomment if you want TensorRT acceleration

# PyTorch with CUDA 12.1 — install via:
# pip install torch==2.3.0+cu121 torchvision==0.18.0+cu121 --index-url https://download.pytorch.org/whl/cu121
